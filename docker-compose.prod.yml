services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    restart: always
    command: ["sh", "-e", "server_runner.sh"]
    volumes:
      - .:/app
      - ./media:/app/media
      - ./static:/app/static
      - ./media:/app/media
    networks:
      - app-network

  postgres:
    image: postgres:18-alpine
    env_file:
      - ./.env
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    networks:
      - app-network
    restart: always

  nginx:
      image: nginx:1.29
      container_name: nginx-server
      ports:
        - "80:80"
      volumes:
        - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
        - ./staticfiles:/app/static
      restart: always
      networks:
        - app-network
      depends_on:
        - backend

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data: